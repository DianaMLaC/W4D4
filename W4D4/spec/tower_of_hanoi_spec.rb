describe TowerOfHanoi do
    
    describe "initialize" do
        it "sets the first tower" do
            expect(tower1.type).to eq([5,4,3,2,1])
        do

        it "sets the second tower to an empty array" do
            expect(tower2.length).to eq(0)
        end

        it "sets the third tower to an empty array" do
            expect(tower3.length).to eq(0)
        end
    end

    describe "move" do
    end

